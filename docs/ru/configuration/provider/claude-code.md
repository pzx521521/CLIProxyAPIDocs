# Claude Code (Anthropic через OAuth):

```bash
./cli-proxy-api --claude-login
```

Опции: добавьте `--no-browser`, чтобы вывести URL для входа вместо открытия браузера. Локальный OAuth callback использует port `54545`.