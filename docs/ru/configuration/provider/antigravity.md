# Antigravity (Antigravity через OAuth):

```bash
./cli-proxy-api --antigravity-login
```

Локальный OAuth callback использует port `51121`.

Опции: добавьте `--no-browser`, чтобы вывести URL для входа вместо открытия браузера. Локальный OAuth callback использует port `51121`.