# Codex (OpenAI via OAuth):

```bash
./cli-proxy-api --codex-login
```

Опции: добавьте `--no-browser`, чтобы вывести URL для входа вместо открытия браузера. Локальный OAuth callback использует port `1455`.