# iFlow (iFlow через OAuth):

```bash
./cli-proxy-api --iflow-login
```

Опции: добавьте `--no-browser`, чтобы вывести URL для входа вместо открытия браузера. Локальный OAuth callback использует port `11451`.