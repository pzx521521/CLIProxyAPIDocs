# Qwen (Qwen Chat через OAuth):

```bash
./cli-proxy-api --qwen-login
```

Опции: добавьте `--no-browser`, чтобы вывести URL для входа вместо открытия браузера. Используйте OAuth device flow от Qwen Chat.